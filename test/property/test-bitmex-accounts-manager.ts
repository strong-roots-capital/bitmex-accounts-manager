import { testProp, fc } from 'ava-fast-check'


/**
 * Library under test
 */

import { bitmexAccountsManager } from '../../src/bitmex-accounts-manager'


testProp.skip(
    'TODO: property-test bitmex-accounts-manager',
    [
        // arbitraries
    ],
    (
        // test arguments
    ) => {
        // return assertion
    }
)
